import {request} from '../utils/request';import type {CustomMoodTag,Mood,MoodTag} from '../types';export const listMoods=(date?:string)=>request<Mood[]>(`/moods${date?`?date=${date}`:''}`);export const saveMood=(payload:{mood_level:number;mood_tags:MoodTag[];note:string;record_date:string})=>request<Mood>('/moods',{method:'POST',body:JSON.stringify(payload)});export const updateMood=(id:number,payload:{mood_level:number;mood_tags:MoodTag[];note:string;record_date:string})=>request<Mood>(`/moods/${id}`,{method:'PUT',body:JSON.stringify(payload)});export const deleteMood=(id:number)=>request(`/moods/${id}`,{method:'DELETE'});export const listCustomTags=()=>request<CustomMoodTag[]>('/mood-tags');export const createCustomTag=(name:string)=>request<CustomMoodTag>('/mood-tags',{method:'POST',body:JSON.stringify({name})});export const disableCustomTag=(id:number)=>request(`/mood-tags/${id}`,{method:'DELETE'});
